## AutoScript.

### Install :

```
apt update -y && apt upgrade -y && apt install -y screen && wget -q https://raw.githubusercontent.com/KbmnGlntr/AutoScript/main/install.sh && chmod +x install.sh && screen -S RS ./install.sh
```
